#include <iostream>

using namespace std;

int main()
{

    int a, b;
    cout << "Informe o valor de a: ";
    cin >> a;

    cout << "Informe o valor de b: ";
    cin >> b;

    int soma = a + b;

    cout << "A soma entre " << a << " e " << b << " é igual a : " << soma << endl;

    return 0;
}